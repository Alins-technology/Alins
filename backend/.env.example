PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Optional — leave blank to skip email sending (submissions are still saved).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_TO=contact@alins.in

# Optional — set this to enable GET /api/contact (list submissions).
ADMIN_KEY=
